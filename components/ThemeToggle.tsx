'use client';

// ThemeToggle is not used in the Stitch editorial design system.
// The offset.io editorial design uses a fixed light cream palette (#fcf9f8) — dark mode is not supported.
// This component is preserved as a no-op stub.
export default function ThemeToggle() {
  return null;
}
